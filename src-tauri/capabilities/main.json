{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Only the bundled main window can call the native desktop bridge.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
