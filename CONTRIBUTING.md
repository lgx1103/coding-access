# 贡献指南

欢迎提交问题和改进。先说明具体使用场景、预期行为和当前行为；已有功能的稳定性和清晰交互优先于增加设置项。

- 报告问题前注明服务端、客户端、系统和编程工具版本，并删除日志中的凭证与私人内容。
- 修改协议转换时补充声明、调用、返回、多轮历史和流式测试；不要通过丢弃未知字段掩盖不兼容。
- 修改权限、调度或数据库时补充边界、失败和回退用例。
- 界面遵循简洁表单、清楚的状态和可恢复操作，说明必须与真实行为一致。
- 服务端和原生客户端独立版本；不要为了单端修复更改另一端版本。

提交前运行 `npm ci`、`npm test`、`npm run build`。原生客户端改动另外运行 `npm run test:tauri` 和 `npm run check:tauri`，附上目标系统的验证结果。不要提交 `.env`、本地状态、数据库、签名私钥、安装包或内部截图。

开发用演示账号和 fixture 密钥只能用于测试。请保持提交范围清楚，并在 PR 中说明问题、改动、验证及限制。
